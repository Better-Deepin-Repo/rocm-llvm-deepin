Description: Enable building rocrtst as part of rocr
 This change enables building rocrtst as part of the main ROCr source
 tree alongside the runtime and libhsakmt components.

 Previously, rocrtst was maintained within the source tree but intended
 to be built as a separate project. This made it difficult to integrate
 into Debian/Ubuntu packaging workflows, where all buildable components
 must be produced from a single source package.

 With this change:
 - rocrtst can be built conditionally via the BUILD_ROCRTST option.
 - It is integrated into the main CMake build alongside runtime and
   libhsakmt.
 - This allows autopkgtests to be executed against the built artifacts
   without requiring a separate upstream build process.
 - System-installed hsa-runtime64 can still be used when BUILD_ROCRTST
   is disabled, preserving compatibility with external builds and/or
   installations.

 This change is intended to support Debian/Ubuntu packaging by ensuring
 that rocrtst tests are built and runnable within the same source
 package, enabling proper autopkgtest coverage.

 [Talha Can Havadar]
 Refresh for upstream 7.2.3

  Notes:
    - find_package(LLVM REQUIRED CONFIG) requires `llvm-dev` in
      Build-Depends (provides LLVMConfig.cmake and the LLVM_LIBRARY_DIR
      / LLVM_VERSION_MAJOR variables used above).
    - The previous patch's hunks targeting the upstream
      AMDDeviceLibs_FOUND / HWLOC_FOUND blocks were dropped: those
      blocks were introduced by patch 0015 (no longer in the series)
      and never existed in the upstream tarball.

Author: Bojan Aleksovski <bojan.aleksovski@canonical.com>
Bug-Ubuntu: https://bugs.launchpad.net/ubuntu/+source/rocr-runtime/+bug/2147200
Forwarded: https://github.com/ROCm/rocm-systems/pull/4734
Last-Update: 2026-05-19

--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -57,6 +57,8 @@
   set(BUILD_ROCR ON)
 endif()
 
+option( BUILD_ROCRTST "Build ${PROJECT_NAME} tests" OFF )
+
 function(add_rocm_subdir subdir subdir_assigns)
     message("add_rocm_subdir() -- " ${subdir})
     # message("  subdir_assigns before:" ${subdir_assigns} "EOM")
@@ -341,3 +343,9 @@
 cpack_add_component(asan
   DISPLAY_NAME "ASAN"
   DESCRIPTION "ASAN libraries for rocr-runtime")
+
+# Build runtime tests
+if( BUILD_ROCRTST )
+  message(STATUS "Adding rocrtst/suites/test_common subdir")
+  add_subdirectory(rocrtst/suites/test_common)
+endif()
--- a/rocrtst/suites/test_common/CMakeLists.txt
+++ b/rocrtst/suites/test_common/CMakeLists.txt
@@ -131,19 +131,46 @@
 #
 
 # Required Defines first:
-find_package(hsa-runtime64 REQUIRED)
-find_package(amd_smi REQUIRED)
+# Handle two cases
+# 1 - rocrtst is being built separately (default, BUILD_ROCRTST = OFF)
+# 2 - rocrtst is being built together with runtime and libhsakmt (BUILD_ROCRTST = ON)
+# In case 2, it might be built on other archs than amd64. If that is the case, do not
+# break the entire build, but just skip building rocrtst as it depends amd_smi which is
+# available only for amd64 for the time being
+
+if (BUILD_ROCRTST)
+    message(STATUS "Using in-tree hsa-runtime64")
+    set(ROCRTST_LIBS ${ROCRTST_LIBS} hsa-runtime64)
+else()
+  find_package(hsa-runtime64 REQUIRED)
+  message(STATUS "Using system hsa-runtime64 library")
+  set(ROCRTST_LIBS ${ROCRTST_LIBS} hsa-runtime64::hsa-runtime64)
+endif()
+
+if (BUILD_ROCRTST)
+  find_package(amd_smi QUIET)
+  if(NOT amd_smi_FOUND)
+    message(WARNING "Tests (rocrtst) building will be skipped due to amd_smi missing.")
+    return()
+  else()
+  endif()
+else()
+  find_package(amd_smi REQUIRED)
+endif()
+set(ROCRTST_LIBS ${ROCRTST_LIBS} amd_smi)
 
 set (ONLY64STR "64")
 
-if (DEFINED LLVM_DIR)
-  set(CLANG ${LLVM_DIR}/clang)
+find_package(LLVM REQUIRED CONFIG)
+
+if (DEFINED LLVM_TOOLS_BINARY_DIR)
+  set(CLANG ${LLVM_TOOLS_BINARY_DIR}/clang)
   if (NOT EXISTS ${CLANG})
-    message("ERROR: path to clang (${CLANG}) is not valid. Is define LLVM_DIR correct?")
+    message("ERROR: path to clang (${CLANG}) is not valid. Is LLVM_TOOLS_BINARY_DIR correct?")
     return()
   endif()
 else()
-    message("WARNING: LLVM_DIR define is not set. Kernels will not be built.")
+    message("WARNING: LLVM_TOOLS_BINARY_DIR is not set. Kernels will not be built.")
 endif()
 
 if (DEFINED OPENCL_DIR)
@@ -163,6 +190,8 @@
 if(NOT EXISTS "${OPENCL_INC_DIR}/opencl-c.h")
   if(DEFINED ENV{LLVM_PROJECT_ROOT})
     set(OPENCL_INC_DIR "$ENV{LLVM_PROJECT_ROOT}/clang/lib/Headers/")
+  elseif(EXISTS "${LLVM_LIBRARY_DIR}/clang/${LLVM_VERSION_MAJOR}/include/opencl-c.h")
+    set(OPENCL_INC_DIR "${LLVM_LIBRARY_DIR}/clang/${LLVM_VERSION_MAJOR}/include")
   else()
     set(OPENCL_INC_DIR "${OPENCL_DIR}/../../../external/llvm-project/clang/lib/Headers/")
   endif()
@@ -189,13 +218,14 @@
 
 find_path(BITCODE_DIR NAMES "opencl.bc" "opencl.amdgcn.bc"
   PATHS
+    "${LLVM_LIBRARY_DIR}/clang/${LLVM_VERSION_MAJOR}/amdgcn/bitcode"
     "${ROCM_DIR}/amdgcn/bitcode"
     "${ROCM_DIR}/lib/bitcode"
     "${ROCM_DIR}/lib"
     "${ROCM_DIR}/lib/x86_64/bitcode"
     "${OPENCL_DIR}/amdgcn/bitcode"
     "${OPENCL_DIR}/lib/x86_64/bitcode"
-    "${LLVM_DIR}/../lib/bitcode"
+    "${LLVM_LIBRARY_DIR}/bitcode"
     "${CMAKE_PREFIX_PATH}/amdgcn/bitcode"
     "${CMAKE_PREFIX_PATH}/lib/bitcode"
     "${CMAKE_PREFIX_PATH}/lib/x86_64/bitcode")
@@ -264,12 +294,20 @@
 
 set(ROCRTST_ROOT ${CMAKE_CURRENT_SOURCE_DIR}/../..)
 
+#
+# Try using system GTest. Otherwise
 # Set Name for Google Test Framework and build it as a
 # static library to be linked with user test programs
 #
-set(GOOGLE_TEST_FRWK_NAME "google-test-frwk${ONLY64STR}")
-add_subdirectory(${ROCRTST_ROOT}/gtest "${PROJECT_BINARY_DIR}/gtest" EXCLUDE_FROM_ALL)
-set (ROCRTST_LIBS ${ROCRTST_LIBS} ${GOOGLE_TEST_FRWK_NAME})
+find_package(GTest)
+if(GTest_FOUND)
+  set(ROCRTST_LIBS ${ROCRTST_LIBS} GTest::GTest GTest::Main)
+  message(STATUS "Using system GTest library")
+else()
+  set(GOOGLE_TEST_FRWK_NAME "google-test-frwk${ONLY64STR}")
+  add_subdirectory(${ROCRTST_ROOT}/gtest "${PROJECT_BINARY_DIR}/gtest" EXCLUDE_FROM_ALL)
+  set (ROCRTST_LIBS ${ROCRTST_LIBS} ${GOOGLE_TEST_FRWK_NAME})
+endif()
 
 MESSAGE("ROCRTST_LIBS SET STEP:Done")
 #
@@ -277,12 +315,6 @@
 # Other source directories
 aux_source_directory(${ROCRTST_ROOT}/common common_srcs)
 
-#
-# Extend the list of libraries to be used for linking ROC Perf Apps
-#
-set(ROCRTST_LIBS ${ROCRTST_LIBS} hsa-runtime64::hsa-runtime64)
-set(ROCRTST_LIBS ${ROCRTST_LIBS} amd_smi)
-
 
 # Set Name for rocrtst
 MESSAGE(${ROCRTST_LIBS})
@@ -412,7 +444,14 @@
 add_executable(${ROCRTST} ${performanceSources} ${functionalSources} ${negativeSources} ${stressSources}
                                            ${common_srcs} ${testCommonSources})
 
-target_link_libraries(${ROCRTST} ${ROCRTST_LIBS} c stdc++ dl pthread rt numa ${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/lib/libhwloc.so.5)
+if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/lib/libhwloc.so.5")
+  set(HWLOC_BUNDLED TRUE)
+  set(HWLOC_LINK "${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/lib/libhwloc.so.5")
+else()
+  set(HWLOC_BUNDLED FALSE)
+  set(HWLOC_LINK hwloc)
+endif()
+target_link_libraries(${ROCRTST} ${ROCRTST_LIBS} c stdc++ dl pthread rt numa ${HWLOC_LINK})
 
 #Build kernels
 add_custom_target(rocrtst_kernels ALL DEPENDS ${HSACO_TARG_LIST})
@@ -429,6 +468,8 @@
         LIBRARY DESTINATION lib
         RUNTIME DESTINATION bin)
 
-install ( DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/lib DESTINATION lib/rocrtst )
+if(HWLOC_BUNDLED)
+  install ( DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/../../thirdparty/lib DESTINATION lib/rocrtst )
+endif()
 
 include ( CPack )

diff --git a/runtime/hsa-runtime/inc/hsa b/runtime/hsa-runtime/inc/hsa
new file mode 120000
index 0000000..945c9b4
--- /dev/null
+++ b/runtime/hsa-runtime/inc/hsa
@@ -0,0 +1 @@
+.
\ No newline at end of file
